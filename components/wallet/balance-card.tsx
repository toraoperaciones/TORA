/**
 * Deprecado: usa MoneyHero (components/ui/money-hero.tsx).
 * Re-export por compatibilidad — se elimina cuando no queden consumers.
 */
export { MoneyHero as BalanceCard } from "@/components/ui/money-hero";
