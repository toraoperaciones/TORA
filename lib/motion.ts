/**
 * Motion TORA — el easing institucional: out-expo, 100–300ms.
 * Único export vivo (usado por stagger, sparkline, animated-check y shell);
 * el resto del sistema anima con estos mismos valores inline.
 */
export const EASE_TORA: [number, number, number, number] = [0.16, 1, 0.3, 1];
